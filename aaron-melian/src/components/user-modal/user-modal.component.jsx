// React
import React from "react";

// Components
import Backrop from "../backdrop/backdrop.component";

const UserModal = ({ show, cancelUserModal }) => {
  return <Backrop show={show} />;
};

export default UserModal;
